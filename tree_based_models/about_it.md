As the name tells, these models work with tree structures, like there is the root to which all the branches are conntected and to that branches leaves are connected, the same principle works here, but each term is a condition, at each step we face a condition. 
- These tress are used for both classification and regression. 
- There are majorly 2 types in it:
    - Decision tree
    - Random forest 